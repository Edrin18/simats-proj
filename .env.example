# Create a .env.example file
env_example = '''# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# Flask Secret Key (change this to a random string)
SECRET_KEY=simats-secret-key-2024-change-in-production

# SMS API Key (from fast2sms.com) - Optional for testing
SMS_API_KEY=your_fast2sms_api_key_here

# Google OAuth Credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
'''

with open('/mnt/kimi/output/simats_hub/.env.example', 'w') as f:
    f.write(env_example)

# Update .gitignore to include .env
with open('/mnt/kimi/output/simats_hub/.gitignore', 'r') as f:
    gitignore_content = f.read()